# RouteLab TS status

**Base:** `cdc5a83` — numerical split runtime integrated  
**Target:** `v0.1.0` portfolio release  
**Completed:** `PORT-001` cleanup; `PORT-002` facade; `PORT-003` CLI/package
**Active task:** `PORT-004 — Compact benchmark and report`

The retained core provides exact `bigint` pool/replay semantics, bounded route and pool-disjoint split discovery, immutable prepared snapshots, exact fallback behavior, and an exactly authorized numerical allocator.

Next: add the curated deterministic-quality and in-process latency benchmark, concise committed report, verifier, and SVG. Live execution, signing, settlement, PRIME, ML, gas-aware routing, and concentrated liquidity remain out of scope.
