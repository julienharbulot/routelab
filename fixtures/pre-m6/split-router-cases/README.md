# Pre-M6 split router cases

These two canonical, offline cases are fixed fixture evidence for the composed exact-input split runtime. They show the hand-audited `100 -> 50` direct fallback and `50/50 -> 66` split result under explicit typed work caps.

They are not a performance benchmark, a claim of unrestricted global optimality, or evidence of live service, transaction, custody, or protocol execution.
